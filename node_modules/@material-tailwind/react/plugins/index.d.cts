import * as tailwindcss_types_config from 'tailwindcss/types/config';

interface Color {
    default?: `#${string}`;
    dark?: `#${string}`;
    light?: `#${string}`;
    foreground?: `#${string}`;
}
interface Colors {
    background?: `#${string}`;
    foreground?: `#${string}`;
    black?: `#${string}`;
    white?: `#${string}`;
    surface?: Color;
    primary?: Color;
    secondary?: Color;
    info?: Color;
    success?: Color;
    warning?: Color;
    error?: Color;
}
interface Options {
    fonts?: Record<string, string | string[]>;
    radius?: string;
    colors?: Colors;
    darkColors?: Colors;
}
declare const mtConfig: {
    (options: Options): {
        handler: tailwindcss_types_config.PluginCreator;
        config?: Partial<tailwindcss_types_config.Config> | undefined;
    };
    __isOptionsFunction: true;
};

export { Color, Colors, Options, mtConfig as default, mtConfig };
