import { DeepPartial } from '../types/general.js';
import 'react';

type ListTheme = DeepPartial<typeof listTheme>;
type ListItemTheme = DeepPartial<typeof listItemTheme>;
type ListItemStartTheme = DeepPartial<typeof listItemStartTheme>;
type ListItemEndTheme = DeepPartial<typeof listItemEndTheme>;
declare const listTheme: {
    baseStyle: string;
};
declare const listItemTheme: {
    defaultProps: {
        ripple: boolean;
    };
    baseStyle: string;
};
declare const listItemStartTheme: {
    baseStyle: string;
};
declare const listItemEndTheme: {
    baseStyle: string;
};

export { ListItemEndTheme, ListItemStartTheme, ListItemTheme, ListTheme, listItemEndTheme, listItemStartTheme, listItemTheme, listTheme };
